/**
 * Personalization feature — the feedback bar under assistant replies that
 * captures router corrections (see src-tauri/src/router/personalization.rs
 * and commands/route.rs's submit_router_correction).
 */

export { FeedbackBar } from "./FeedbackBar";
